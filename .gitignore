# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
# .python-version IS committed: local dev, CI, and the Docker image must all
# run the same interpreter (pinned 3.13; image is python:3.13-slim).

# uv
.uv/

# Build artifacts
dist/
build/
*.egg

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Env + secrets
.env
.env.local
*.local

# Data + state (mounted volumes in prod)
/data/
/config/
*.db
*.db-journal
*.db-shm
*.db-wal

# Frontend
frontend/node_modules/
frontend/dist/
src/despereaux/static/reader/

# Testing
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
# Smoke-tier throwaway container state (docker-compose.ci.yml volumes)
.smoke/

# Logs
*.log

# Internal handoff / AI-context notes — kept locally, not published.
HANDOFF.md
NOTES.md
.claude/
